package com.example.englishup.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background ──
val Bg = Color(0xFF0A0E1A)
val Bg2 = Color(0xFF111827)
val Bg3 = Color(0xFF1A2235)

// ── Surface ──
val Surface = Color(0xFF1E2D45)
val Surface2 = Color(0xFF253650)

// ── Border ──
val Border = Color(0x12FFFFFF)   // 7%
val Border2 = Color(0x24FFFFFF)  // 14%

// ── Text ──
val TextPrimary = Color(0xFFF0F4FF)
val TextSecondary = Color(0xFF8A9BBF)
val TextTertiary = Color(0xFF4A5A7A)

// ── Accent: Green ──
val Green = Color(0xFF00E5A0)
val GreenDim = Color(0x1F00E5A0)   // 12%
val GreenMid = Color(0x4000E5A0)   // 25%

// ── Accent: Red ──
val Red = Color(0xFFFF4D6A)
val RedDim = Color(0x1FFF4D6A)

// ── Accent: Amber ──
val Amber = Color(0xFFFFB547)
val AmberDim = Color(0x1FFFB547)

// ── Accent: Blue ──
val Blue = Color(0xFF4D9FFF)
val BlueDim = Color(0x1F4D9FFF)

// ── Accent: Purple ──
val Purple = Color(0xFFA78BFA)
val PurpleDim = Color(0x1FA78BFA)